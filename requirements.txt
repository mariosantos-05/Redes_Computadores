# Este projeto foi desenvolvido utilizando apenas bibliotecas nativas do Python (Standard Library),
# como 'asyncio', 'json', 'socket', 'logging', entre outras.
# Portanto, nenhuma biblioteca externa de terceiros é estritamente necessária para executar o chat P2P ou o servidor Rendezvous.

# Se desejar configurar ferramentas de desenvolvimento recomendadas no seu ambiente virtual (venv), 
# você pode descomentar as linhas abaixo e instalá-las com: pip install -r requirements.txt

# Ferramenta para formatação automática de código
# black>=24.0.0

# Verificador estático de tipos (útil devido à tipagem opcional de typing)
# mypy>=1.8.0

# Ferramenta de linting para verificar conformidade com PEP 8
# flake8>=7.0.0
